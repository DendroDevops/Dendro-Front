import { FilterEssencePipe } from './filter-essence.pipe';

describe('FilterEssencePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEssencePipe();
    expect(pipe).toBeTruthy();
  });
});
