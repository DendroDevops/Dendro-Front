import { FilterPlantNamePipe } from './filter-plant-name.pipe';

describe('FilterPlantNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlantNamePipe();
    expect(pipe).toBeTruthy();
  });
});
